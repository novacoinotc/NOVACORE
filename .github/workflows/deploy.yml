name: Deploy to AWS EC2

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      GH_PAT: ${{ secrets.GH_PAT }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy to EC2
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USERNAME }}
          key: ${{ secrets.EC2_SSH_KEY }}
          envs: GH_PAT
          script: |
            set -e
            echo "🚀 Starting deployment of NOVACORP..."

            cd /home/ubuntu/NOVACORE

            echo "📦 Saving local changes..."
            git add -A
            git stash || true

            echo "🔧 Configuring git authentication..."
            # Configure git to use token for this repository
            git config --local url."https://${GH_PAT}@github.com/".insteadOf "https://github.com/"

            echo "⬇️  Fetching latest changes from GitHub..."
            git fetch origin main

            echo "🔄 Resetting to match GitHub (force sync)..."
            git reset --hard origin/main

            echo "🔧 Removing temporary git config..."
            git config --local --unset url."https://${GH_PAT}@github.com/".insteadOf || true

            echo "📚 Installing dependencies..."
            npm install

            echo "🗄️  Generating Prisma client..."
            npx prisma generate

            # Note: Skipping 'prisma db push' since we use raw SQL queries in db.ts
            # The database schema is managed manually or through SQL migrations

            echo "🔨 Building application..."
            npm run build

            echo "🔄 Restarting PM2..."
            pm2 restart novacorp 2>/dev/null || pm2 start ecosystem.config.js

            echo "✅ Deployment completed!"
            pm2 status
            pm2 save
